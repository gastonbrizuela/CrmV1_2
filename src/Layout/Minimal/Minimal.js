import React from 'react'
import './Minimal.css'

const Minimal = ({children})=>{
    return(
        <div className='content-minimal-view'>
            {children}
        </div>
    )
}
export default Minimal